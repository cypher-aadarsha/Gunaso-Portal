# Core Django and REST framework
Django>=4.2
djangorestframework
django-cors-headers
Pillow # <-- ADDED: Required for ImageField and file uploads (like the ID)
openpyxl
django-jazzmin
django-celery-results   # Stores Celery task results in DB
#Celery and Redis for background tasks
Pillow
celery
redis
google-generativeai
google-api-python-client

# Database
#sqlite3         # Default for development
# If you move to PostgreSQL for production, you would add:
psycopg2-binary
dj-database-url

# Production & Utilities
gunicorn       # Production-ready web server (WSGI)
whitenoise     # Serves static files efficiently
python-dotenv  # For managing .env files (to hide your API key)